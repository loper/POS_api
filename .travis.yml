language: bash

branches:
  only:
  - master

services:
  - docker

before_install:
  - docker build -t pos_ver_api POS_version_API

script:
  - docker run -it -d --rm j-worker:v8 /bin/sh -c "flask --version"

